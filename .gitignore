.venv/
__pycache__/
*.pyc
.env
data/cache/
data/state.json
.DS_Store
.pytest_cache/
logs/*.log
logs/*.err
preview-frames/

# Runtime state. Excluded from deploys too, so the copy in git was always
# both stale and the wrong shape for anyone else -- see data/*.example.json.
data/overrides.json
data/reminders.json
